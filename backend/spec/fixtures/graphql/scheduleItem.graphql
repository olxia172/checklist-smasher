mutation scheduleItem($id: ID!, $scheduleData: ScheduleDataAttributes!) {
  scheduleItem(input: { id: $id, scheduleData: $scheduleData }) {
    item {
      id
    }
    schedule {
      id
    }
  }
}
